# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.env
.env.production

# editors
.vscode/
.idea/
.DS_Store

# playwright artifacts
.playwright-mcp/

# скриншоты проверок в корне (в src/assets .jpeg не трогаем)
/*.jpeg

# Directus payloads: fixture binaries are committed so CI builds without Directus,
# `public/media` is regenerated from them by `prebuild` and stays out of the repo.
# Контент из Directus: файлы фикстуры лежат в репозитории, иначе сборка на CI
# падает без Directus; `public/media` собирается из них в `prebuild` и не хранится.
public/media/

# настройки Claude Code
.claude/

# лог dev-сервера, запущенного отдельным процессом
.astro-dev.log*

# снимки чужих сайтов для разбора вкуса, в репозиторий не идут
.refs/
.refs2/

# локальные снимки и скрипты проверки, сборки параллельных проходов
.shots/
.build-*/
