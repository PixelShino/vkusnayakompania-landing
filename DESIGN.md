# DESIGN.md — «Вкусная компания»

Идентичность зафиксирована макетом (`handoff/`). Это не greenfield: палитра,
шрифт и радиусы уже выбраны и утверждены, менять их нельзя. Документ описывает
систему, чтобы новые блоки не расходились со старыми.

## Цвет

Стратегия — **committed**: тёплый кремовый фон несёт всю страницу, оливковый
работает как второй голос (карточки-акценты), терракота — как редкий сигнал
(цифры, ссылки в магазин).

| Роль | Значение | Где |
|---|---|---|
| Фон | `#FFFAED` | страница целиком |
| Поверхность | `#F6EEE3` | футер, текстовая карточка первого экрана |
| Панель | `#F1EFE0` | «торт по вашей картинке» |
| Плейсхолдер фото | `#EFE7DA` | пока не загрузилось |
| Текст | `#2C2927` | заголовки, кнопки-заливки, круги-номера |
| Текст 2/3/4 | `#5A5348` · `#6B6252` · `#8C8272` | лиды, мета, подписи |
| Оливковый | `#868C56` | карточка часов, «торт на заказ», «написать нам» |
| Оливковый тёмный | `#5F6439` | текст на светло-оливковом |
| Терракота | `#C67139`, текст `#8C4C24` | цифры бенто, ссылки в магазин |
| Линии | `#E4DBC9` · `#E9DECC` · `#EFE5D6` | рамки карточек, разделители |

Чёрный и белый не используются: даже тёмный текст тёплый (`#2C2927`).

## Типографика

**Montserrat Variable** (кириллица), одна семья. Контраст держится весом:
заголовки `800` с трекингом `-0.02em` и интерлиньяжем `1.07`, тело `400/600`
с `1.55`.

| Шаг | 390 | 834 | 1440 |
|---|---|---|---|
| H1 | 34 | 47 | 66 |
| H2 | 25 | 33 | 42 |
| H3 | 17 | 19 | 21 |
| Тело | 14 | 15 | 16 |
| Лид | 15 | 17 | 19 |

Между контрольными точками — `clamp()`. Заголовки ломаются по `text-wrap:
balance`, абзацы — по `pretty`. Цифры в телефонах и часах табличные.

## Ритм

Боковой отступ 18 / 36 / 72, отступ секции 44 / 64 / 92, шаг сетки 12 / 16 / 20.
Контент 354 / 762 / 1296. Брейкпоинты — `768px` и `1200px`.

## Формы

Радиусы: `999px` — кнопки, пилюли, круги-номера. `14–16px` — мелкие ячейки и
фото. `20–24px` — карточки, карта, фото первого экрана. `26–32px` — большая
панель. Тени почти нет: глубина строится заливкой и рамкой `1px`.

## Повторяющийся паттерн

Шапка карточки из трёх элементов: **круг** (номер, буква, иконка) → **растянутый
пунктир** `2px dashed` → **пилюля-статус** справа. Держит всю страницу: «как
заказать», отзывы, контакты, перелинковка. Новые блоки строятся по нему же.

## Кнопки

Высоты: 38 (мелкая) · 42 (в карточках) · 46 (основная) · 48 (на первом экране,
мобильный). Всегда `999px`. Тёмная заливка — главное действие, обводка —
второстепенное, светлая заливка — действие на оливковом фоне.

## Фотография

Продукт снят крупно и тепло, кадры не осветляются и не «моются». Соотношения:
3:4 — торты, 1:1 — десерты, 4:3 — детали зала, 16:7 — общий план, 4:5 — пара
«картинка → готовый торт». Подпись лежит **на** фото поверх градиента снизу.

Просмотра во весь экран нет: снимки и так крупные, а слой поверх страницы только
уводит от заказа.

## Движение

Сдержанное: переходы 0.16s на цвет и границу, никаких въездов при скролле.
Плавность даёт скролл-снап рельсов и раскрытие сетки, а не анимации.

## Чего в системе нет

Теней-подложек, стеклянных панелей, градиентного текста, полосок-акцентов
слева у карточек, иконок в кружках над каждым заголовком.
