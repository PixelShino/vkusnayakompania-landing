---
import MessengerButtons from './MessengerButtons.astro';
import { site, places } from '../data/site';

const pickup = places.map((p) => `${p.street} — ${p.opensAt}`).join(', ');
---

<section class="section" id="how">
  <div class="container">
    <div class="section-head">
      <div>
        <p class="kicker">Три способа</p>
        <h2>Как заказать</h2>
        <p class="lead">
          Забрать с витрины, привезти домой или собрать торт под событие — выбирайте по срокам.
        </p>
      </div>
      <a class="how__phone" href={site.phoneHref}>{site.phone}</a>
    </div>

    <div class="how__grid">
      <article class="how__card">
        <div class="card-top">
          <span class="card-mark">1</span>
          <span class="card-dash"></span>
          <span class="card-pill">20–40 минут</span>
        </div>
        <h3 class="how__title">Доставка</h3>
        <p class="how__text">
          Каталог, корзина и оплата — в отдельном магазине. Есть приложения для iOS и Android.
        </p>
        <div class="how__foot">
          <a class="btn btn--secondary btn--md" href={site.links.shop}>В магазин</a>
        </div>
      </article>

      <article class="how__card">
        <div class="card-top">
          <span class="card-mark">2</span>
          <span class="card-dash"></span>
          <span class="card-pill">Сегодня</span>
        </div>
        <h3 class="how__title">Самовывоз</h3>
        <p class="how__text">
          То, что стоит на витрине, можно просто забрать. {pickup}.
        </p>
        <div class="how__foot">
          <a class="btn btn--secondary btn--md" href="#contacts">Адреса и часы</a>
        </div>
      </article>

      <article class="how__card how__card--hi">
        <div class="blob" aria-hidden="true"></div>
        <div class="card-top">
          <span class="card-mark card-mark--light">3</span>
          <span class="card-dash card-dash--light"></span>
          <span class="card-pill card-pill--light">48 часов</span>
        </div>
        <h3 class="how__title">Торт на заказ</h3>
        <p class="how__text">
          Готовая модель или своя картинка. Меняем цвет и надпись, считаем стоимость в тот же день.
        </p>
        <div class="how__foot">
          <MessengerButtons variant="light" size="md" />
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .how__phone {
    font-size: var(--fs-body);
    font-weight: 600;
    color: var(--terracotta-text);
    text-decoration: none;
    white-space: nowrap;
  }

  .how__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: calc(var(--gap) + 2px);
    margin-top: 26px;
    align-items: stretch;
  }

  .how__card {
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    padding: calc(var(--gap) + 10px);
    border: 1px solid var(--line);
    border-radius: var(--r-xl);
    background: var(--bg);
  }

  .how__card--hi {
    border-color: #7c8250;
    background: var(--olive);
    color: var(--bg);
  }

  .how__title {
    position: relative;
    z-index: 1;
    margin: 20px 0 8px;
    font-size: calc(var(--fs-h3) + 3px);
    line-height: 1.12;
  }

  .how__text {
    position: relative;
    z-index: 1;
    font-size: var(--fs-body);
    line-height: 1.55;
    color: var(--text-2);
  }

  .how__card--hi .how__text {
    color: rgba(255, 250, 237, 0.86);
  }

  .how__foot {
    position: relative;
    z-index: 1;
    margin-top: auto;
    padding-top: 18px;
  }

  @media (min-width: 768px) {
    .how__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
